import * as React from 'react';

import { MultiFactorAuthenticationSkeletonCard } from '@/components/dashboard/settings/account/security/multi-factor-authentication-skeleton-card';

export default function MultiFactorAuthenticationLoading(): React.JSX.Element {
  return <MultiFactorAuthenticationSkeletonCard />;
}
